# 8. Write a Python program to check a list is empty or not. 
# program8.py

lst = [1, 2, 3]
if len(lst) == 0:
    print(f"Given list is empty as its length is {len(lst)}")
else:
    print(f"lst length is: {len(lst)}, so it is not empty")

